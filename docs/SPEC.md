# CANTUS Specification v0.1

## Execution Model
Stack-based virtual machine driven by audio tokens.

## Opcode Mapping
C = PUSH
D = ADD
E = SUB
F = MUL
G = DIV
A = JUMP
B = IF_ZERO

Accidentals:
C# DUP
D# SWAP
F# DROP
G# OVER
A# ROT
